version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
customRedirects:
  - source: '/<*>'
    target: '/index.html'
    status: '200'
